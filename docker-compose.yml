backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000" # <-- Add this (change 5000 to whatever port your backend uses)
    volumes:
      - backend_data:/app/data
    restart: unless-stopped
